# =============================================================================
# Dotfiles .gitignore
# =============================================================================
# CRITICAL: Secrets must NEVER be committed to git

# Secrets and tokens (actual values, not scripts)
secrets.fish
.env
.env.*
*.key
*.pem
*.p12
*.pfx
*secret.txt
*secrets.txt
*secret.json
*secrets.json

# SSH keys (never commit private keys!)
.ssh/id_*
.ssh/*.pem
.ssh/known_hosts

# 1Password items
.op/

# API credentials (actual files with secrets, not scripts)
*token.txt
*token.json
*password.txt
*password.json
*credential.txt
*credential.json

# Backup files
*.backup
*.backup.*
.*.swp
.*.swo
*~

# OS files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# IDE
.vscode/
.idea/
*.sublime-*

# Logs
*.log

# Temporary files
tmp/
temp/
